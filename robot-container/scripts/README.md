# robot-container/scripts/

**Purpose:** operational scripts that support the container but aren't part of the application itself — Docker entrypoints, local dev helpers, simulation bootstrap scripts.

**Will contain:** `entrypoint.sh` (container startup), plus any scripts for running the agent or simulation locally outside Docker during development.

**Filled in:** Milestone 2 onward, as each piece needs a way to be started/tested.
