# robot-container/docs/

**Purpose:** implementation notes specific to the robot container that are too low-level for the top-level [`docs/`](../../docs/) learning path (e.g. ROS2 node graphs, GStreamer pipeline details, Gazebo world configuration).

For the conceptual, step-by-step explanation of *why* this container is built the way it is, see the top-level [`docs/`](../../docs/) folder instead — start at [`docs/README.md`](../../docs/README.md).

**Filled in:** as needed from Milestone 4 onward.
