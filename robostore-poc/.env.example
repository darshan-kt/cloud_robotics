# Real-time gateway ROBOSTORE's live-data hooks will eventually talk to.
# Unused today - no app currently built here needs live robot data yet. See
# README.md and ../docs/configuration-reference.md for what this becomes in
# Phase 2 (pointing at the existing cloud-container backend instead of a new
# bespoke gateway).
VITE_GATEWAY_URL=http://localhost:1717
