# cloud-container/docs/

**Purpose:** implementation notes specific to the cloud container that are too low-level for the top-level [`docs/`](../../docs/) learning path (e.g. database schema details, MQTT service internals).

For the conceptual, step-by-step explanation of *why* this container is built the way it is, see the top-level [`docs/`](../../docs/) folder instead — start at [`docs/README.md`](../../docs/README.md).

**Filled in:** as needed from Milestone 7 onward.
