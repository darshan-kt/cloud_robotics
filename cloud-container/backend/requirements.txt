fastapi==0.141.1
uvicorn[standard]==0.30.6
pydantic==2.9.2
PyYAML==6.0.2
# Milestone 7: fleet manager, robot registry, session manager, MQTT
# service, auth - see cloud-container/backend/README.md.
paho-mqtt==1.6.1
asyncpg==0.29.0
redis==5.0.8
# Bumped from 2.9.0 (Milestone 12 dependency audit - pip-audit flagged
# known CVEs in that version, fixed here). fastapi bumped alongside it to
# pull in a starlette release with its own CVEs fixed, since fastapi==0.115.0
# pinned to a vulnerable starlette transitively - see docs/12-security-hardening.md.
PyJWT==2.13.0
