# cloud-container/docker/

**Purpose:** container build assets for the cloud-side images.

**Will contain:** a `Dockerfile` for the FastAPI backend and a multi-stage `Dockerfile` for the React frontend (build with Vite, serve statically), plus any shared build resources.

**Filled in:** Milestone 2.
